services:
  # The Frontend Service (Vue + Nginx)
  frontend:
    build: ./short-ui
    ports:
      - "3000:3000"
    restart: always
    networks:
      - app-network

networks:
  app-network:
    driver: bridge